# Miner components

Human-friendly entry points for miner-specific behavior. Current implementations reuse the mature operations/update services; new component-specific logic should migrate here instead of growing a universal file.
