import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('Lingui and Babel i18n foundation is configured',()=>{const p=JSON.parse(read('package.json')),c=read('lingui.config.js'),b=read('build.mjs'),r=read('web/i18n/index.js');assert.equal(p.dependencies['@lingui/core'],'6.6.0');assert.equal(p.devDependencies['@lingui/cli'],'6.6.0');assert.equal(p.devDependencies['@lingui/format-po'],'6.6.0');assert.equal(p.devDependencies['@lingui/babel-plugin-lingui-macro'],'6.6.0');assert.equal(p.devDependencies.esbuild,'0.28.1');assert.match(c,/sourceLocale:\s*'en'/);assert.match(c,/locales:\s*\['en',\s*'ru'\]/);assert.match(c,/@lingui\/format-po/);assert.match(c,/format:\s*formatter\(/);assert.match(b,/transformAsync/);assert.match(r,/i18n\.load\('en'/);assert.match(r,/i18n\.activate/);});
test('language switch remains visible and wired on login and sidebar',()=>{const h=read('web/index.html'),r=read('web/i18n/index.js');assert.match(h,/RU/);assert.match(h,/EN/);assert.ok(r.includes("closest?.('[data-locale]')"));assert.ok(r.includes("addEventListener('click',onLocaleClick)"));assert.ok(r.includes("setLocale(button.dataset.locale)"));assert.ok(r.includes("classList.toggle('active',active)"));assert.ok(r.includes("aria-pressed"));});
test('socket connection label follows live state across locale changes',()=>{const h=read('web/index.html'),m=read('web/app/main.js');assert.doesNotMatch(h,/id="socket-state"[^>]*data-i18n/);assert.ok(m.includes('function renderSocketState()'));assert.ok(m.includes('Boolean(socket?.connected)'));assert.ok(m.includes("socket.on('connect',renderSocketState)"));assert.ok(m.includes("socket.on('disconnect',renderSocketState)"));assert.ok(m.includes('onLocaleChange(()=>{renderSocketState();'));});
test('translations live in separate Lingui PO catalogs',()=>{assert.ok(fs.existsSync(path.join(root,'web/locales/en/messages.po')));assert.ok(fs.existsSync(path.join(root,'web/locales/ru/messages.po')));assert.equal(fs.existsSync(path.join(root,'web/i18n/en.js')),false);assert.equal(fs.existsSync(path.join(root,'web/i18n/ru.js')),false);});
test('server dialogs are outside the small composition root',()=>{const m=read('web/app/main.js');assert.doesNotMatch(m,/function\s+openServerForm\s*\(/);assert.doesNotMatch(m,/function\s+bootstrapModal\s*\(/);assert.match(m,/createServerDialogs/);});
