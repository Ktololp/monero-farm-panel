import { msg } from '@lingui/core/macro';

export default {
  "api.backendUnavailable": msg({ id: "api.backendUnavailable", message: "Lost connection to the panel backend. Check the START_WINDOWS window and data/panel-crash.log." }),
  "api.loginRequired": msg({ id: "api.loginRequired", message: "Login required" }),
};
