import { msg } from '@lingui/core/macro';

export default {
  "socket.connected": msg({ id: "socket.connected", message: "connected" }),
  "socket.disconnected": msg({ id: "socket.disconnected", message: "disconnected" }),
};
